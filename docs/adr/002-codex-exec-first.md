# ADR-002: Codex Exec first

Status: Accepted

The first real adapter uses `codex exec --json` behind a replaceable worker protocol. Its flags are
validated against installed help and official documentation. Live tests remain explicit opt-in.

