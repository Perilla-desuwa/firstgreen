# Add authentication audit logging

Emit audit records after successful login and after successful password reset. Each record must contain the user ID, event type, and timestamp. Add focused tests for both flows.
