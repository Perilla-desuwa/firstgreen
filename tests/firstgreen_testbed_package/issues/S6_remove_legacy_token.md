# Remove legacy token field

Remove the deprecated `legacy_token` field from persisted user data and clean up all application references. Include the required schema migration and verification steps. Do not lose existing user records.
