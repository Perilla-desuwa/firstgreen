# Add order cancellation

Add a `cancelled` order state and a cancellation workflow. A pending order can be cancelled, completed or already-cancelled orders cannot be cancelled, and a successful cancellation sends a fake email. Add focused service and integration tests.
